import React from 'react';
import ReactDOM from 'react-dom';

const style = {
    color: 'darkblue',
    padding: '30px',
    backgroundColor: 'deeppink'
}

ReactDOM.render(<div style={style}>Remote Query Consumer Package</div>, document.querySelector('#app'));